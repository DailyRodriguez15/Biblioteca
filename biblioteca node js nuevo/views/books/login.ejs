<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Iniciar Sesión - Mi Biblioteca Virtual</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <style>
      /* Fondo con textura tipo madera o libro antiguo */
      body {
        min-height: 100vh;
        background: linear-gradient(135deg, #5d432c 0%, #8b6b4a 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Georgia", serif;
        color: #3a2e1a;
        margin: 0;
      }

      /* Contenedor login con efecto sombra y borde suave */
      .login-container {
        background: #f7f1e1;
        border-radius: 1.5rem;
        box-shadow: 0 12px 40px rgba(92, 68, 37, 0.5);
        padding: 3rem 2.5rem;
        max-width: 420px;
        width: 100%;
        text-align: center;
        border: 3px solid #7a5c34;
      }

      /* Logo o icono arriba */
      .logo {
        font-size: 4rem;
        color: #7a5c34;
        margin-bottom: 1rem;
      }

      /* Título con tipografía elegante */
      .login-title {
        font-weight: 700;
        font-size: 2.5rem;
        margin-bottom: 2rem;
        letter-spacing: 0.08em;
        text-shadow: 1px 1px 3px #b29473;
      }

      /* Inputs con bordes redondeados y color cálido */
      .form-control {
        border-radius: 2rem;
        padding: 0.85rem 1.5rem;
        font-size: 1.15rem;
        border: 2px solid #7a5c34;
        background: #fff8f0;
        color: #5d432c;
        transition: all 0.3s ease;
      }

      .form-control:focus {
        border-color: #a88244;
        box-shadow: 0 0 8px rgba(168, 130, 68, 0.6);
        background: #fff;
        color: #5d432c;
        outline: none;
      }

      /* Botón con gradiente marrón y efecto hover */
      .btn-login {
        background: linear-gradient(90deg, #7a5c34 40%, #a88244 100%);
        color: #fff;
        border: none;
        border-radius: 2rem;
        padding: 0.75rem 2.5rem;
        font-weight: 700;
        font-size: 1.2rem;
        margin-top: 1.8rem;
        box-shadow: 0 6px 15px rgba(122, 92, 52, 0.7);
        transition: background 0.3s ease;
        width: 100%;
      }

      .btn-login:hover {
        background: linear-gradient(90deg, #a88244 40%, #7a5c34 100%);
        color: #fff;
      }

      /* Link para contraseña olvidada */
      .forgot-link {
        display: block;
        margin-top: 1.3rem;
        color: #7a5c34;
        font-weight: 600;
        text-decoration: none;
        transition: color 0.25s ease;
      }

      .forgot-link:hover {
        color: #a88244;
        text-decoration: underline;
      }

      /* Texto para registro */
      .form-text {
        margin-top: 2rem;
        font-size: 1rem;
        color: #5d432c;
      }

      .form-text a {
        color: #7a5c34;
        font-weight: 700;
        text-decoration: none;
        transition: color 0.3s ease;
      }

      .form-text a:hover {
        color: #a88244;
        text-decoration: underline;
      }
      .alert-danger {
        position: relative;
        background-color: #b02a37; /* Rojo vino elegante */
        color: #ffffff;
        border-radius: 0.75rem;
        padding: 1rem 1.5rem 1rem 3rem;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        font-size: 1rem;
        font-weight: 500;
        margin-bottom: 1.5rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
      }

      /* "X" decorativa a la izquierda */
      .alert-danger::before {
        content: "✖";
        position: absolute;
        left: 1rem;
        top: 50%;
        transform: translateY(-50%);
        font-size: 1.1rem;
        font-weight: bold;
        color: #ffffff;
        opacity: 0.8;
      }

      .alert-success {
        background: #4c8c4a;
        color: #fff;
        border-radius: 1rem;
        border: none;
        padding: 0.75rem 1.2rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        box-shadow: 0 0 8px rgba(76, 140, 74, 0.8);
      }
    </style>
  </head>

  <body>
    <div class="login-container">
      <div class="logo">
        <i class="bi bi-book-half"></i>
      </div>
      <div class="login-title">Iniciar Sesión</div>

      <% if (messages && messages.error) { %>
      <div class="alert alert-danger"><%= messages.error %></div>
      <% } %> <% if (messages && messages.success) { %>
      <div class="alert alert-success"><%= messages.success %></div>
      <% } %>

      <form action="/books/login" method="post" autocomplete="on" novalidate>
        <div class="mb-3">
          <input
            type="email"
            class="form-control"
            name="email"
            id="email"
            placeholder="Correo electrónico"
            autocomplete="email"
            required
            autofocus
          />
        </div>
        <div class="mb-3">
          <input
            type="password"
            class="form-control"
            name="password"
            id="password"
            placeholder="Contraseña"
            autocomplete="current-password"
            required
          />
        </div>
        <button type="submit" class="btn btn-login">
          <i class="bi bi-box-arrow-in-right me-2"></i> Entrar
        </button>
      </form>

      <a href="/books/recuperar" class="back-link">
        <i class="bi bi-question-circle me-1"></i> ¿Olvidaste tu contraseña?
      </a>

      <div class="form-text">
        ¿No tienes cuenta? <a href="/books/registro">Regístrate</a>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
