<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Mi Biblioteca Virtual</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <style>
      :root {
        --library-brown: #52331a;
        --library-brown-dark: #332012;
        --library-white: #ffffff;
        --library-brown-light: #e3d3c2;
      }

      body {
        background: var(--library-white);
        font-family: "Segoe UI", "Georgia", serif;
        color: var(--library-brown-dark);
      }

      .navbar-glass {
        backdrop-filter: blur(10px);
        background-color: var(--library-brown) !important;
        border-bottom: 1.5px solid var(--library-brown-light);
      }

      .navbar-glass .navbar-brand,
      .navbar-glass .nav-link,
      .navbar-glass .navbar-toggler-icon {
        color: #fff !important;
      }

      .navbar-glass .nav-link.active,
      .navbar-glass .nav-link:focus,
      .navbar-glass .nav-link:hover {
        color: var(--library-brown) !important;
        background: var(--library-white);
        border-radius: 7px;
        font-weight: 600;
      }

      .nav-link {
        font-weight: 500;
        letter-spacing: 0.5px;
      }

      /* Sección bienvenida */
      .welcome-section {
        max-width: 950px;
        margin: 6rem auto 4rem auto;
        background: var(--library-brown-light);
        border-radius: 18px;
        padding: 3.2rem 2.2rem;
        text-align: center;
        box-shadow: 0 8px 32px rgba(82, 51, 26, 0.1);
        border: 2px solid var(--library-brown);
      }

      .welcome-section h1 {
        font-weight: 700;
        font-size: 2.85rem;
        color: var(--library-brown);
        margin-bottom: 1.1rem;
        letter-spacing: 1px;
      }

      .welcome-section p {
        font-size: 1.23rem;
        color: var(--library-brown-dark);
        margin-bottom: 2.2rem;
      }

      .btn-welcome {
        background: var(--library-brown);
        border: none;
        color: #fff;
        font-weight: 700;
        padding: 0.8rem 2.1rem;
        border-radius: 50px;
        font-size: 1.13rem;
        transition: background 0.3s, box-shadow 0.3s;
        box-shadow: 0 6px 14px rgba(82, 51, 26, 0.14);
        letter-spacing: 0.5px;
      }

      .btn-welcome:hover {
        background: var(--library-brown-dark);
        color: #fff;
        box-shadow: 0 9px 22px rgba(82, 51, 26, 0.22);
      }

      /* Sección Libros Destacados */
      .featured-books {
        max-width: 1120px;
        margin: 3rem auto 4rem auto;
      }

      .featured-books h2 {
        font-weight: 700;
        margin-bottom: 2.2rem;
        color: var(--library-brown-dark);
        text-align: center;
        letter-spacing: 0.5px;
      }

      .card-dark {
        background: var(--library-brown-dark);
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 7px 22px rgba(82, 51, 26, 0.12);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        color: #fff;
        cursor: pointer;
        border: 1.5px solid var(--library-brown-light);
        min-height: 410px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .card-dark:hover {
        transform: translateY(-11px) scale(1.03);
        box-shadow: 0 16px 32px rgba(82, 51, 26, 0.22);
      }

      .card-dark img {
        width: 100%;
        height: 220px;
        object-fit: cover;
        border-bottom: 3px solid var(--library-brown);
        background: var(--library-white);
      }

      .card-dark .card-body {
        padding: 1.5rem 1.2rem;
        flex: 1 1 auto;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .card-dark .card-title {
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 0.7rem;
        color: #fff;
        letter-spacing: 0.2px;
      }

      .card-dark .card-text {
        font-size: 1.03rem;
        opacity: 0.92;
        margin-bottom: 1.15rem;
        line-height: 1.5;
        color: #fff;
      }

      .btn-custom {
        background: var(--library-brown);
        border: none;
        color: #fff;
        font-weight: 600;
        padding: 0.53rem 1.3rem;
        border-radius: 50px;
        transition: background 0.3s, box-shadow 0.3s;
        box-shadow: 0 4px 12px rgba(82, 51, 26, 0.15);
        font-size: 1rem;
        letter-spacing: 0.2px;
      }

      .btn-custom:hover {
        background: var(--library-brown-dark);
        color: #fff;
        box-shadow: 0 8px 20px rgba(82, 51, 26, 0.19);
      }

      /* Footer */
      footer {
        background-color: var(--library-brown-dark);
        color: #fff;
        padding: 2.1rem 0 1.3rem 0;
        text-align: center;
        font-size: 1.02rem;
        font-weight: 500;
        box-shadow: inset 0 3px 10px rgba(245, 239, 230, 0.08);
        letter-spacing: 0.2px;
        border-top: 2px solid var(--library-brown-light);
      }

      footer a {
        color: var(--library-brown-light);
        text-decoration: none;
        font-weight: 600;
        transition: color 0.3s;
      }

      footer a:hover {
        color: #fff;
        text-decoration: underline;
      }

      /* Empuja el botón al extremo derecho con ms-auto en el contenedor */

      .btn-welcome {
        background: var(--library-white);
        border: 2px solid var(--library-brown);
        color: var(--library-brown);
        font-weight: 700;
        padding: 0.5rem 1.8rem;
        border-radius: 50px;
        font-size: 1rem;
        transition: background 0.3s ease, color 0.3s ease, box-shadow 0.3s ease;
        letter-spacing: 0.5px;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        box-shadow: 0 4px 10px rgba(82, 51, 26, 0.12);
        cursor: pointer;
      }

      .btn-welcome:hover {
        background: var(--library-brown-dark);
        color: var(--library-white);
        box-shadow: 0 8px 18px rgba(82, 51, 26, 0.22);
        border-color: var(--library-brown-dark);
      }
    </style>
  </head>

  <body>
    <!-- 🌐 Menú Moderno Glass -->
    <nav
      class="navbar navbar-expand-lg navbar-dark navbar-glass fixed-top shadow-sm"
    >
      <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="#">
          <i class="bi bi-book-half fs-4 me-2"></i> Mi Biblioteca Virtual
        </a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#menuNavbar"
          aria-controls="menuNavbar"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="menuNavbar">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" href="#">
                <i class="bi bi-house-door-fill me-1"></i>Inicio
              </a>
            </li>
            <li class="nav-item ms-3">
              <a class="nav-link" href="/books/acerca">
                <i class="bi bi-info-circle-fill me-1"></i>Acerca
              </a>
            </li>
          </ul>
          <!-- Botón Iniciar Sesión a la derecha, pegado al borde -->
          <div class="d-flex justify-content-end">
            <a href="/books/login" class="btn btn-welcome">
              <i class="bi bi-journal-richtext me-1"></i> Iniciar Sesión
            </a>
          </div>
        </div>
      </div>
    </nav>

    <!-- Sección de Bienvenida -->
    <section class="welcome-section">
      <h1>Bienvenido a Mi Biblioteca Virtual</h1>
      <p>Descubre, organiza y comparte tus libros favoritos con facilidad.</p>
    </section>

    <!-- 📚 Nueva Sección de Libros Destacados Mejorada -->
    <section class="featured-books">
      <h2>📖 Libros Destacados</h2>
      <div class="row g-4">
        <div class="col-md-4">
          <div class="card-dark">
            <img
              src="/stylesheets/img/bi1.webp"
              alt="El Viaje del Lector"
              loading="lazy"
            />
            <div class="card-body">
              <h5 class="card-title">El Viaje del Lector</h5>
              <p class="card-text">
                Una aventura a través de mundos literarios fascinantes.
              </p>
              <button class="btn btn-custom">
                <i class="bi bi-bookmark-check me-1"></i> Más Información
              </button>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="card-dark">
            <img
              src="/stylesheets/img/El-liderazgo-del-bibliotecario-escolar.jpg"
              alt="Liderazgo Escolar"
              loading="lazy"
            />
            <div class="card-body">
              <h5 class="card-title">Liderazgo Escolar</h5>
              <p class="card-text">
                Estrategias clave para guiar comunidades educativas.
              </p>
              <button class="btn btn-custom">
                <i class="bi bi-bookmark-check me-1"></i> Más Información
              </button>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="card-dark">
            <img
              src="/stylesheets/img/Maia-Habegger.jpg"
              alt="Historias que Inspiran"
              loading="lazy"
            />
            <div class="card-body">
              <h5 class="card-title">Historias que Inspiran</h5>
              <p class="card-text">
                Narrativas poderosas que despiertan la imaginación.
              </p>
              <button class="btn btn-custom">
                <i class="bi bi-bookmark-check me-1"></i> Más Información
              </button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer>
      <p>© 2025 Mi Biblioteca Virtual. Todos los derechos reservados.</p>
      <p>
        Síguenos en
        <a href="#" aria-label="Facebook"
          ><i class="bi bi-facebook"></i> Facebook</a
        >,
        <a href="#" aria-label="Twitter"
          ><i class="bi bi-twitter"></i> Twitter</a
        >
        y
        <a href="#" aria-label="Instagram"
          ><i class="bi bi-instagram"></i> Instagram</a
        >
      </p>
    </footer>

    <!-- JS Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
